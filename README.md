# SC Project 3

## Description

This project implements a Python program to solve systems of linear equations using Gaussian elimination with scaled partial pivoting. The program outputs intermediate steps to both the terminal and a file.

## Usage

To install the libraries for the project, run the following command:
(or just use a virtual environment)

```sh
pip install -r requirements.txt
```